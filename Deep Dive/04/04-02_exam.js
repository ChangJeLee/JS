var userId = 1;
var userName = 'Lee';

var user = {
    id: 1,
    name: 'Lee',
};

var users = [
    {id:1, name: 'Lee'},
    {id:2, name: 'Hyo'}
];

var result = 30;

//변수에 값을 할당(=대입, 저장)이라 하고, 변수에 저장된 값을 읽어 들이는 것을 참조(reference)라 함
//result에 저장해서 나중에 값을 불러오는 것을 식별자 라고 함 result와 30의 메모리 주소값이 매핑되어 있음 result --> 0x~~~ 포인터 라고 생각5 ==